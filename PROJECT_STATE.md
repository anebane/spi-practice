# PROJECT STATE（プロジェクト状態）

> このファイルは、ClaudeCode / OpenCode / LMStudio のどのツールでも同じ文脈から開発を再開するための「正本」です。
> 開発の区切りごとに必ず更新する。終わったら「次にやること」と「設計意図」を必ず残す。
> 参照: `DIVISION_OF_LABOR.md`（役割分担）

## 概要
SPI非言語を無料で練習できる模擬試験サービス（適性検査無限ドリル）。問題はパラメータランダム生成で無限出題。WebApp（PWA・静的なHTML/CSS/JS）。

- 公開URL: https://tekisei-drill.com/（GitHub Pages）
- GA4: G-SPDZ1K30TB / AdSense: ca-pub-5409685648363967
- 収益: 広告 + アフィリエイト（a8.net、記事ページ中心）

## 技術スタック
- 素のHTML/CSS/JS（フレームワークなし）・PWA（Service Worker・manifest）
- 問題: `src/questions/*.js`（13分野）→ `tools/build-questions.js` で `questions.js` に結合
- テスト: Node製の自前spec群 + CI（GitHub Actions）
- 計測: `analytics.js`（記事ページ / 読了・CTA送客）、GA4スクリプト

## デプロイ・CI
- GitHub Actions `test.yml`: main push / PR で以下を実行
  1. `questions.js` が `src/questions/` と一致するか（build-questions.js 再生成チェック）
  2. 問題ジェネレータ検証（79テンプレ x 1000回）
  3. HTML健全性（未展開変数・内部リンク）
  4. 広告枠の不変条件（PR表記・案件注記・属性の枠）＝ affiliate.spec.js
  5. 記事ページ計測（reader達成）＝ analytics.spec.js
  6. 検査の登録漏れ（CIと破壊テスト両方）＝ wiring.spec.js
  7. PWA構成（manifest・アイコン・プリキャッシュ）＝ pwa.spec.js
  8. 導線のE2E（?cat= リンク）＝ deeplink.spec.js
  9. 破壊テスト（検査が本当に落ちるか）＝ mutation-runner.js
  10. 計測整合（exam_start と exam_finish の対応）＝ app.spec.js
- 破壊テスト: `test/mutations.json`（壊し方）を適用し、指定specが落ちることを検証。「検査を足したら変異も足す」義務を機械化。カバーできない失敗経路は `test/mutations-uncovered.json` に理由つきで登録（台帳は縮める方向にのみ動かす）
- 破壊テストの隔離実行（2026-09-06）: mutation-runner は起動時に作業ツリーのスナップショット（未コミット含む）を一時 git worktree に展開し、その中で変異・復元を行う。**実行中もメインの作業ツリーは自由に編集してよい**（09-04/09-06 の編集衝突4回の再発防止）。`--check-ledger` / `--check-mutations` / 集約は従来どおりその場で読むだけ

## 自律環境の状況（2026-09-01 構築・push済み）
夜間自律開発の環境一式を **main にコミットし、`origin/main` へpush済み**（`anebane/spi-practice`）。

- `CLAUDE.md` — 開発文化・検証手順・自律ルール・**ClaudeCodeトークン復活時の段取り**
- `PROJECT_STATE.md` — このファイル（状態の正本）
- `DIVISION_OF_LABOR.md` — ClaudeCode / OpenCode / LMStudio の役割分担
- `.claude/settings.json` — 安全コマンドの許可/確認/拒否
- `.claude/agents/devel-secure.md` — 夜間自律用の安全エージェント
- `tools/nightly.sh` — 起動ランチャー（`run` / `--check`）。`--check`が `AVAILABLE` を返せばClaudeCode復活済み
- `docs/nightly/` — 夜間作業レポート格納先（git管理）
- launchd `com.demae.spi.nightly` — 毎晩0時に自動実行（有効化済み `enabled`）。ClaudeCode上限中はスキップしてログに残す

## 現在のブランチ状態

⚠️ **ここに一覧を手で書かない。**2026-09-02に「未マージ5ブランチ・28コミット」と書かれていたが、
実測すると**5本とも `origin/main` の祖先**で、未マージは0本だった。
書いた瞬間から古くなる情報を正本に置くと、読んだ全員が同じ誤りから始める。

**測り方**（これを実行して判断する）:

```sh
git fetch origin
for b in $(git branch --format='%(refname:short)' | grep -v '^main$'); do
  if git merge-base --is-ancestor "$b" origin/main; then
    echo "$b  ✅ main に取り込み済み（削除してよい）"
  else
    echo "$b  ⚠️ main に無いコミットが $(git log --oneline origin/main..$b | wc -l) 本"
  fi
done
```

**2026-09-02 の実測**: `independent-recalc` / `merge-recalc` / `sweep-props` / `weak-probe` /
`ledger-triage` の5本とも main の祖先。中身は全部 main に入って本番に出ている。
ブランチ名だけが古い位置に残っている状態。**削除しても情報は失われない**（判断は矢野さん）。

## 直近の評価（2026-09-01 時点）
- 強み: 品質文化（変異テスト）は個人開発として突出。ドメイン設計（ランダム生成・分割管理）は優れている。収益化・SEO考慮も入っている
- 懸念: 開発重心が「機能・稼働」より「テストのテスト」に偏り気味。稼働実績・指標の確認が不明
- ⚠️ **「5ブランチ放置」は事実誤認**（2026-09-02に実測・上記参照）。放置は0本で、作業は全部 main に入り本番に出ている。
  実際に出たもの: 間違えた入力への「正解」表示の修正（濃度算6件・6ヶ月間生存）／分野数の嘘17件／
  存在しない解き方の宣伝5件／広告枠 1面→13面／案件 3件→4件（¥22,500追加）

## 今夜のタスク（夜間セッションはこれ「だけ」を実行する）

> ⚠️ 夜間セッションは**1件だけ**実行して止まる。終わって時間が余っても次を探さない。
> 2026-08-29〜30に2日で週次上限へ到達し、その後3日間ほぼ何もできなかった。
> 次のタスクは朝に指定する。**夜間セッションはこの欄を書き換えない。**

### 損益算7テンプレに「問題文から独立に解き直す検査」を書く

**背景**: `generator.spec.js` の検算は「解説の式どうしが整合しているか」しか見ておらず、
**答えが問題文の題意と合っているかは誰も見ていなかった**（2026-08-30に実測）。
`answerFormula` に `+1` を入れても全specが緑のままだった。

**済んでいる家族**（同じやり方を踏襲する）: 濃度算7 / 四則逆算7 / 表5 / 仕事算6 / 集合6 = 31件
**今回の対象**: 損益算7件（`src/questions/04-soneki.js`）

**絶対条件**:
- ⚠️ **`answerFormula` を読んでから検査を書かない。**同じ式が2箇所になるだけで、
  式が間違っていたら両方とも同じように間違う。**検査は「利用者が読む問題文」から答えを導く**
- 作業順序で担保する: 検査を書く → 素の緑を確認 → **その後に初めて** `answerFormula` を
  プローブの照準としてだけ見る

**受け入れ条件（これが合格の全て）**:
```
7テンプレそれぞれの answerFormula に +1 を入れると、
「独立再計算と答えが不一致」のメッセージが出て EXIT=1 になること
```
⚠️ `EXIT=1` だけでは不足。**狙ったメッセージが出るところまで**確認する。

**書けない形が出たら**: 無理に通さず「この形は問題文から解き直せない」と
レポートに個別の理由を書く。推論系と同じ手が使えない領域があるはず。

**触ってよいファイル**: `src/questions/04-soneki.js` / `test/generator.spec.js` /
`test/mutations.json`（検査を足したら変異も足す）
**触らないファイル**: `test.yml` / `test/mutation-runner.js` / `affiliate.js` / HTML

⚠️ **2026-09-02 の補足**: この日、コードを変えたことで既存変異の `find` が
0箇所に一致し、黙って効かなくなる事故が起きた（`1470428` で修正）。
**全件実行しないと分からない**（単独実行では緑のまま）。足したあと必ず全件を回すこと。

## 2026-09-02 に分かったこと（実測）

### 利用状況

```
直近28日 2,655セッション
  自然検索(google)  976 (37%)
  ⚠️ AI経由         545 (21%)   8/24を境に増加。8/27と8/30はGoogleを上回った
  直接・不明        492 (19%)
  自然検索(bing)    445 (17%)
```

**AI経由は質も高い**（完走率 52.2% > 自然検索 45.1%、1人あたり解答 9.5問 > 7.9問）。
**AIはツールそのものを紹介している**——AI経由の着地はトップページ97%で、
記事・分野ページは6セッションのみ。**記事を増やしてもAIO効果は薄い可能性が高い。**

⚠️ 「AIOの効果は測れない」と判断しかけたが、GA4 が `sessionMedium` に
`ai-assistant` を自動で付けていた。`project-scanner/scan.py` の週次レポートに
「流入元（AI経由が測れるか）」の節を追加済み（0件なら警告を出す・検査19ケース）。

### 試験中の時間切れが多い

```
question_timeout 268 対 完走 279（ほぼ同数）
難易度別の中央値  難1: 3.5% / 難2: 5.2% / 難3: 7.0%
```

原因は「難しさ」ではなく**無限小数の四捨五入**。`noudo_basic_01` は答えの94.2%が
無限小数（450g+40g → 8.1633…%）で、さらに「水の量 ≠ 食塩水の量」の引っかけが重なる。
実際のSPI例題は 4.5% / 12.5% / 8% など**割り切れる**小数で、無限小数は出ていない。

**制限時間で吸収する判断をした**（44d48e0）。引っかけは本番にある論点なので消さず、
多様性も削らない（割り切れる組だけにすると81通り→5通り）。

⚠️ **`sokudo_basic_01` の時間切れ11.6%は未解明。**割り切れているのに高い。

### 完走した人の行動

```
完走 279 → 解説を見直した 130 (47%) → もう一度挑戦 45 (16%) → シェア 0 (0%)
新規 201ユーザー(1人1.08回) / リピート 58ユーザー(1人2.72回)
```

**戻る人は平均2.7回使う。**「無限に出題」はリピーターには効いている。
**関心は「もう一度解く」より「間違いを理解する」**（47% > 16%）。

⚠️ **シェアが4日間0件。**機能を畳む判断もありうる。

## 書けなかった検査（次の人が同じ壁に当たらないように）

**1. 四捨五入を要求するテンプレの制限時間**
「四捨五入 ＋ 同難易度で最短 → 時間切れが増える」という代理指標で書いたが、
`wariai_change_01` が両方を満たしながら時間切れ4.8%（中央値以下）で、
**指標が実態を説明できなかった**ため撤回。時間切れ率はGA4でしか分からない。

**2. 弱点CTAのクリック記録**
ハーネスの `querySelectorAll` はセレクタごとに手で分岐を並べる作りで `.cta-btn` に
未対応。クラスセレクタ対応を足しても、**返るのは新しく作った偽要素**で、
`app.js` がリスナを付けた実体とは別物のためクリックが記録されない。
**「検査があるのに何も見ていない」を作るので撤回。**

⚠️ **どちらも「守る検査が無い」まま変更が入っている。**戻されても気づけない。

## GA4カスタムディメンションの登録漏れ（繰り返している）

```
placement / creative / audience   2026-09-02 に登録（8/30〜9/2の579表示は分解不可）
⚠️ variant                        未登録。8/27のA/B（再挑戦ボタン2種）が測れていない
```

**実装しても GA4 に登録しないと測れない。**⚠️ **遡及しない**ので、登録が遅れた
ぶんのデータは永久に失われる。**実装と同時に登録すること。**

## 2026-09-03 に直したこと（アフィリエイト枠）

### 本番に出ていた文言の欠陥2件（修正・デプロイ済み）

ASPの管理画面と突き合わせて発見。どちらも既存の検査を通っていた。

| | 本番の記述 | ASPの実際 |
|---|---|---|
| アンカー | 「最短**1**日で内定獲得可能！」 | 「最短**10**日」＝**10倍の誇張**（優良誤認・素材改変） |
| 対象学年 | 「2027年卒・2028年卒」 | 「2026年卒、2027年卒」＝**対象外に出すと全件否認** |

リンク（`BWVTE`）は配布物と同一で、**文言だけが書き換わっていた**。注記がキミスカと同じ文言なので、コピペ時の更新漏れと思われる。希望勤務地の条件（東京/神奈川/千葉/埼玉/大阪/京都/兵庫のみ）も抜けていたので追記した。

**原因は `anchor` と `note` が手書きで、照合する相手が無かったこと。** ASPで見た事実を `verified`（date / anchor / years）として素材に持たせ、`test/affiliate.spec.js` が突き合わせるようにした。注記の学年は**集合として**一致を見る（部分一致だと年を足しても通る）。**未確認の素材は verified を持たず、件数を毎回表示する**（現在4件: kimisuka_spi / kimisuka_general / uzuz_it / uzuz_second）。

⚠️ **キミスカの成果条件は未確認。** neoの欠陥のコピー元にあたるので、ここがずれていれば同じく全件否認になる。A8 `insId=s00000009978003` で確認して `verified` に入れること。

### 計測の分母が間違っていた（修正済み）

`affiliate_view` は **IntersectionObserver を使わず描画時に発火**していた。結果画面のこの枠は分野別成績より下にあるので、**そこまでスクロールしていない人も全員分母に入る**。8/28〜9/1の実測「表示677に対しクリック1＝CTR 0.148%」はその分母の値で、**「見られていない」のか「見られたが押されない」のかを区別できていない**（打ち手が正反対になる）。

`affiliate_visible` を新設（**50%以上が1秒以上**見えたら1回だけ）。`affiliate_view` は**残す**（消すと過去分と比較できなくなる）。実ブラウザで4性質を確認済み＝画面外では飛ばない／50%×1秒で飛ぶ／往復しても1回／0.4秒では飛ばない。

**可視率 = affiliate_visible ÷ affiliate_view** が数日で出る。**それが出るまで「CTRを7倍に」等の目標値は根拠が無い。**

### 実効単価（ASPの管理画面で実測）

| 案件 | 額面 | 確定率 | 実効 | 対象 |
|---|---|---|---|---|
| 新卒就職エージェントneo | ¥22,500 | 35.71% | **¥8,035** | 26卒・27卒／首都圏・関西 |
| UZUZ第二新卒(A8) | ¥9,000 | 53.13% | **¥4,782** | 18〜29歳の既卒・第二新卒 |
| UZUZ 28新卒(AT) | ¥8,000 | 未取得 | — | **28卒の在学生限定** |
| キミスカ | **¥700** | 未取得 | — | 学生 |

**3案件が学年で排他**なので、「学生／既卒」の2分割では粗い。**学年の選択を1回挟む**のが否認回避に効く。UZUZ第二新卒は**A8のほうが¥209高い**（アクセストレード¥8,791）。

### 変異について気づいたこと

**コードを直すと、それを狙っている変異の `find` も一緒に古くなる。** 検査は緑のままなので、**全件実行しないと気づけない**。今日、`affiliate_view` の呼び出しをリファクタし注記を書き換えた結果、既存の変異2件が `find` 一致0箇所で死に、対応する失敗経路2つがカバレッジ未達になっていた（修正済み）。

また **`find` は一意でなければ意味がない**。`      anchor:`（6スペース）は `        anchor:`（8スペース）の部分文字列になるため2箇所に一致し、変異が適用されないまま緑が出る。ランナーがこれを検出した。

**最終結果: 239件すべて検出・未カバー0件。**

## 課題（未着手・優先度つき）

### ⚠️ 1. トップページの検索スニペットがUIの断片になっている

Google の検索結果で、トップページの説明文がこう出ている（2026-09-02 実測）:

```
© 2026 適性検査無限ドリル. SPI非言語 無料模擬試験 β. 残り時間 --:-- -- / --.
分野: -- -- 問題 -- / --. 回答して次へ スキップ 解説を見る...
```

**原因**（実測）: `index.html` の DOM 構造が
```
① 説明文（良い内容）        0〜900字
② フッター                  〜2237字
③ 試験画面のUI（--:-- 等）  2237〜2538字  ← 非表示だがDOM最後尾に存在
```
となっており、Google が ③ を拾っている。meta description は正しく書かれているが
採用されていない。

⚠️ **AIO にも同じ影響がある。**AI も同じ HTML を読むので、
「残り時間 --:-- -- / --」を見てサイトを説明することになる。
AI経由が全流入の19%に達している（下記）ので、SEO だけの問題ではない。

**方向**: 試験画面のUIを DOM 最後尾から動かす／初期HTMLに空欄のプレースホルダを
置かない（JSで生成する）など。⚠️ **どれが効くかは未検証。**変更したら
検索結果とAIの応答が実際に変わるかを測ること。

### 2. 分野別ページ2本がインデックス未登録

```
/categories/soneki/     検出 - インデックス未登録・未クロール
/categories/kakuritsu/  同上
```
内部リンク4本・sitemap登録済み・文字数1,784字と、技術的な不備は無い。
作成が 2026-08-26 で1週間しか経っていないのが最有力の理由。
**手作業**: GSC の URL検査 → インデックス登録をリクエスト（APIは読み取り専用）

### 3. GA4カスタムディメンションの登録漏れ（対応済み・要確認）

`placement` / `creative` / `audience` が未登録で、記事12面に広告枠を広げた
効果を面ごとに分解できなかった。2026-09-02 に登録済み。
⚠️ **カスタムディメンションは遡及しない。**8/30〜9/2 の579表示は永久に分解不可。
**翌日以降、値が入っているかを確認すること**（空のままなら設定を疑う）。

## 次にやること（未決定・ClaudeCodeの意見を聞きたい）
- [ ] **破壊テストの全件実行が「未カバーの失敗経路21件」で赤**（2026-09-06実測）。変異は257件すべて有効（壊すと落ちる）だが、profile.spec.js の19経路と html.spec.js の2経路（252行・257行）はどの変異でも発火しない。profile.spec.js 追加時（18fd4dc）からの積み残しで、push するとCIの集約ジョブが落ちる。変異を書くか、書けない理由を添えて台帳へ（台帳は件数が増えると `--check-ledger` が止めるため、基本は変異を書く）
- [ ] **ClaudeCode復活時: `CLAUDE.md` の「ClaudeCode トークン復活時の段取り」に意見・修正を加える**（矢野さんが確認待ち。CLAUDE.mdに追記済みだが未コミット・判断保留中）
- [ ] **自律環境の初回実走査**（復活後、`./tools/nightly.sh run` で手動テスト→正常確認後にlaunchd常駐に任せる）
- [ ] 未マージ5ブランチの扱い（精算・マージ・捨てる）を判断
- [ ] 稼働データの確認（GSC/GA4、どの記事が流入・送客しているか）
- [ ] 機能・問題数の拡充（品質道具偏重からの回帰）

> ⚠️ 現在の `main` には、自律環境push時に解決していない未コミット変更がある可能性がある。作業前に `git status` を確認すること。

## 設計意図（変更容易な本質）
- 「同じ問題が再び出ない」= パラメータランダム生成が最大の差別化。必ず守る
- 非エンジニア単独開発を前提に、壊れにくさ・巻き戻しやすさを最優先
- 検査は「書いただけ」では信用しない。必ず破壊テスト（変異）で実効性を保証する

## 2026-09-06: 公務員試験の分野を追加（categoryId 15「操作と手順」）

`/koumuin/` 向けに判断推理の頻出型を6本追加（`src/questions/15-sousa.js`）。
年齢算3本（何年後・何年前・和と倍率）/ 時計算（角度）/ 油分け算 / ニセ金貨（天秤）。

- **川渡りは見送り**: 古典は答えが固定でランダム生成にならず、制約を乱数で振ると
  「渡れない・最小回数が曖昧」な盤面ができるため
- **時計算は「角度」のみ**: 「重なる時刻」は答えが 60h/11 分の分数になり数値入力と相性が悪い
- 油分け算の最少回数はBFSで全手順を探索。validate で最少4回以上に絞る
  （絞らないと**3回以下で解ける組が30.7%**・解なしが13.4%混ざる。実測）
- `test/generator.spec.js` に「問題文からの独立再計算（操作と手順）」を追加。
  年齢算は総当たり代入で**解がちょうど1つ**であることも同時に検査。変異3件を登録済み
- 既知の弱点: ニセ金貨は答えの空間が {2,3,4} しかなく、最頻値を答え続けると46%当たる
  （型の性質上避けられない。出題を n=5〜45 に絞って偏りは最小化済み）

## 2026-09-06: エンジンからテンプレ固有の知識を剥がした

### なぜやったか

出題範囲を増やす方針が決まった（**国内の公務員試験**と**英国のSHL型**、さらに今後も増やす）。
判定基準を1つ置いた:

> **新しい展開先を足すときに、`generator.js` と `app.js` を1行も触らずに済むか。**

着手前の実測では触らないと無理だった。`generator.js` に `template.id === "..."` の
分岐が**52箇所**あり、**89本中52本がエンジンを触らないと動かない**状態だった。
展開先が増えるたびにここが伸び続ける構造。

### やったこと

| | 前 | 後 |
|---|---|---|
| `computeDerivedVars` | 495行 | **146行** |
| `template.id ===` の直書き | **52箇所** | **0箇所**（コメントの言及2件のみ） |
| `PROB_PAIRS` のID表 | 7件 | **廃止**（テンプレの `probPair` 宣言で駆動） |
| `generator.js` 全体 | 663行 | **561行** |
| `derive` を持つテンプレ | 0本 | **51本** |

- **派生変数**（解説で使う値）→ テンプレ側の `derive(v, answer)` へ
- **変数生成の制約** → テンプレ側の既存 `resolve(v)` へ
- **共通の派生変数**（`answer` / `ansNum` / `ansDen`）は `commonDerived` としてエンジンが必ず供給する契約に
- `fracStr` / `stepStr` を `src/questions/_base.js` の共有関数へ（テンプレの `derive` から呼べるように）

### ⚠️ 移行を検証するときの落とし穴（実際に踏んだ）

**「テストが緑だから移行できている」は成立しない。旧経路が残っていれば緑になる。**

1. **受け口を入れたつもりが入っていなかった。** 編集が適用されないまま検査が全部緑で、
   旧経路が働いていただけだった。→ **「新しい経路が何回呼ばれたか」を実測して発覚**
2. **`derive` を手とスクリプトの両方から入れて3本が二重定義になった。**
   JSは同じキーを2回書いてもエラーにならず**あとの定義が黙って勝つ**ので、
   生成結果は正しく全検査が緑のまま通った。
   → `test/generator.spec.js` に**キー重複の検査**と変異を追加
3. **共通ブロックを見落として上書きした。** `derive` の戻り値で丸ごと置き換えたため、
   解説の `{{answer}}` が全問未展開になった。→ 共通部分は必ずエンジンが供給する形に

**検証の型**: 旧分岐を消す**前**に、同じ変数・同じ乱数種で新旧を走らせて突き合わせる。
不一致0を確認してから消す（今回は派生変数2,052件・変数生成16,000件で不一致0）。

### 次にやること

- `app.js` の `CATEGORY_PAGES` が13分野固定 ← 展開先ごとに書き換えが要る
- `unit`（14種）と `explanationTemplate`（79本・23,150文字）が日本語直書き ← **英語対応はここ**
- ⚠️ **英語対応は「プロファイル」の器を作ってから。** 先に英語を継ぎ足すと3つ目の展開先で作り直しになる

## 2026-09-06（続き）: 公務員試験への展開と、本体のバグ3件

### 展開先を増やせる形にした
- **出題プロファイル**（`src/questions/_profile.js`）を新設。分野・難易度帯・問題数・
  シェア文言を宣言し、ページ側が `ACTIVE_PROFILE` を宣言して切り替える。
  **`app.js`（1,428行）は複製していない。** SPI固定だったのは3箇所だけだった
- `test/profile.spec.js`（新設）が宣言と実体（テンプレート・画面・categories/）を突き合わせる

### 公務員試験の面を作った
```
/koumuin/                12分野・91テンプレート・難易度[中/難]
categories/seisu/        整数の性質の解き方
categories/sousa/        操作と手順の解き方
articles/koumuin-suuteki-shori.html
```
新規テンプレ13本（整数の性質7・操作と手順6）。答えは独立に検算済み
（年齢算450問・天秤200問・油分け300問をBFSで解き直し、不一致0）。

⚠️ **収益導線は未着手。** 結果画面に出るのは就活・第二新卒向けで、公務員志望者には対象外。
A8の料率確認（手作業）が済むまで触らない。

### ⚠️ 見つかった本体のバグ3件（どれも例外が出ないので気づけない）

**① 試験に出るテンプレートが2割しかなかった**
`catTemplates[i % length]` でファイル順の先頭から固定で採っていた。
20問の試験は1分野1〜2問なので、**96本中76本が一度も出なかった**。
数値はランダムなので問題文は毎回変わり、気づけない。
→ 分野内をシャッフル。使われる型 20/96 → 96/96、難易度3が 5% → 24%

**② 答えが2種類しかない問題があった**
`sokudo_round_01`（往復の平均速度）。調和平均 2ab/(a+b) は**距離に依存しない**のに
距離を振っていたため、問題文だけが変わり答えは48と75の2つだけ。
**2回解けば当たる状態**だった。→ 速度の組を表から選ぶ形に。10種・最頻18.5%

**③ 商標の注記が8ページで欠けていた**
CLAUDE.md の鉄則6に対して**機械の検査が無かった**。人の目で守る決まりは
ページが増えるたびに漏れる。→ 10ページに追加し、検査と変異を足した

### ⚠️ 検査を書くときに繰り返した失敗
- **「spi プロファイルの分野だけを見る」取り違えを5回**した。横断する定義を1箇所に作った
- **`if (!c.slug) continue;` で自分に抜け道を作り、自分で使った**。
  解説ページを作らないまま分野を追加できた。宿題台帳（`test/category-pages-todo.json`）を新設
- **閾値を40→50%に上げたら、別の変異が発火しなくなった**。判定軸を2本立てにした
- **単体で確認したつもりが、その後の変更で崩れていた**。最後は必ず全件実行で確かめる
