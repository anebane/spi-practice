---
name: lean
description: >
  書く前に「そもそも要るか」から順に降りて、最初に成り立つ段で止める。
  過剰実装・不要な抽象・車輪の再発明・新規依存を避け、動く最小で終わらせる。
  spi-practice のコードを書く・直す・足す・レビューするとき常に使う。
  「lean」「ponytail」「過剰実装」「削れるところ」「YAGNI」「もっと短く」
  「シンプルに」「依存を増やしたくない」と言われたときも使う。
  問題文の文章・記事の日本語など、コード以外の執筆には使わない。
---

# lean（怠けたシニアの手順）

怠けるとは、雑にすることではなく、無駄を書かないこと。
**一番良いコードは、書かなくて済んだコード。**

## 常時有効

このリポジトリでコードに触れる間は毎回効いている。迷ったら効いている扱い。
止めるのは「lean やめて」「通常モード」と言われたときだけ。

## はしご（最初に成り立つ段で止める）

1. **そもそも要るか。** 「あとで使うかも」は要らない。要らないと判断したら1行でそう言う
2. **このリポジトリに既にあるか。** `src/questions/*.js` の既存テンプレ、`test/` の既存ヘルパ、
   `app.js`・`generator.js` の既存関数。数ファイル隣にあるものを書き直すのが一番多い無駄
3. **素の JS / Node の標準機能で足りるか。** 足りるなら使う
4. **ブラウザ標準で足りるか。** `<input type="date">`、CSS、`Intl`、`localStorage`。
   このプロジェクトはフレームワーク無しの素の HTML/CSS/JS。**標準に寄せるほど壊れにくい**
5. **既に入っている依存で足りるか。** このリポジトリは実質依存ゼロ。**新規依存を足さない**
6. **1行で済むか。** 済むなら1行
7. **ここで初めて、動く最小を書く。**

2段が同時に成り立つなら上の段を取って先に進む。

## はしごは「理解のあと」に降りる

読むことは怠けない。先に、変更が触る経路を端から端まで辿る。

- `questions.js` を直接編集しない。`src/questions/*.js` を直して `node tools/build-questions.js` で再生成する
- 関数を最後まで読む。途中まで読んで名前を推測しない（実績: `share_click` を探して0件→実際は `share_x`）
- **バグ修正は症状でなく根本原因。** 触る関数の呼び出し元を全部 grep する。
  共有関数に1つガードを置くほうが、呼び出し元ごとに置くより差分が小さい。
  チケットが名指しした経路だけ直すと、兄弟の呼び出し元が壊れたまま残る

理解を飛ばして小さい差分を出すのは、怠惰の悪いほうだ。効率の顔をした2つ目のバグになる。

## 書くときの規則

- 頼まれていない抽象を作らない。実装が1つしかないインタフェース、製品が1つのファクトリ、変わらない値の設定項目
- 「あとで使う」足場を作らない。あとで必要になったら、あとで作れる
- **足すより消す。賢いより退屈。** 賢いコードは3時に誰かが解読する
- ファイル数は少ないほうを取る。差分は短いほうを取る（ただし理解したあとで）
- 標準の書き方が2つあって同じ長さなら、境界条件に強いほうを取る。怠けるのは量であって、精度ではない
- 複雑な依頼が来たら、怠けた版を出したうえで同じ返信で問い返す。「Xをやった。Yで足りている。フル版が要るなら言って」

## 怠けてはいけないところ

**この4つは、短さより優先する。**

- **検査を消す・弱めることで短くしない。** `test/mutations.json` は縮める方向にのみ動かす。
  非自明なロジック（分岐・ループ・判定・金額）を足したら、**壊れたら落ちる最小の検査を1本だけ**残す。
  これは CLAUDE.md の「検査を足したら変異も足す」と同じことを言っている。自明な1行に検査は要らない
- **問題の数値を固定化して短くしない。** ランダム生成による無限出題がこのサービスの差別化。
  丸暗記できる形にした時点で、コードが短くなっても製品は死ぬ
- **法規・商標の表記を削らない。** PR表記・広告の透明性・「SPI」の商標配慮は CI が見ている
- **入力検証・データ喪失を防ぐエラー処理・アクセシビリティ**を削らない

利用者が「フル版が要る」と言ったら作る。言い直して抵抗しない。

## 削るレビューをするとき

差分（またはツリー）を「複雑さ」だけの目で見る。**正しさのバグ・セキュリティ・性能は対象外**。
それらは通常のレビューに回す。1件1行で書く。

`<file>:L<行>: <タグ> <何を削るか>。<代わりに何を置くか>。`

| タグ | 意味 |
|---|---|
| `delete:` | 死んだコード、使われない柔軟性、先回りの機能。代わりは無し |
| `reuse:` | このリポジトリに既にある。どのファイルの何かを名指しする |
| `native:` | ブラウザ/標準がすでにやっている。機能名を名指しする |
| `yagni:` | 実装が1つの抽象、誰も設定しない設定、呼び出し元が1つの層 |
| `shrink:` | 同じ挙動で行数が減る。短いほうの形を書く |

❌「このバリデータは必要以上に複雑かもしれません。すべてのルールが現段階で必要かご検討ください」
✅ `app.js:L12-38: shrink: 27行の手書き判定。1行の正規表現で同値。`

最後に必ず数字で締める（分母を出す規則と同じ）: `N件中M件を削減可能 / net: -N行`。
削るものが無ければ `既に十分短い。そのまま出す。` とだけ書いて止める。適用はしない、列挙するだけ。

## 意図的な手抜きには印を残す

天井が分かっていて割り切った簡略化（総当たり、O(n²)、素朴なヒューリスティック）には、
**天井と、それを外す条件**をコメントに書く。

```js
// lean: 4択のみ対応。5択を出す分野が増えたら分岐を分ける
```

書式は `lean: <天井>、<外す条件>`。回収はこれで済む:

```
grep -rn "// lean:" --include=*.js --include=*.html . | grep -v node_modules
```

**外す条件が書かれていない印は腐る。** 条件を書けないなら、それは手抜きではなく判断保留なので、
`⚠️ 判断がつかない` として報告に残す。

## 出力

コードが先。そのあと最大3行。何を省いたか、いつ足すべきか。
**説明がコードより長くなったら説明を消す。**
簡略化を弁護する段落は、散文の形で戻ってきた複雑さだ。
利用者が明示的に頼んだ説明（レポート・手順の説明）は別。それは全部書く。

形: `[コード] → 省いた: [X]。[Y] になったら足す。`
