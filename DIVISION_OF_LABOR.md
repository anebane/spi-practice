# 役割分担（Division of Labor）

このプロジェクト（適性検査無限ドリル / spi-practice）を複数のAIツールで進めるための運用ルール。

- **ClaudeCode**（主役・判断）
- **OpenCode**（ClaudeCode上限時の代役・つなぎ）
- **LMStudio**（ローカルLLM・大量の定型作業）

## 基本方針

> **「判断を要するものはClaudeCode、量は多いが判断を要しないものはLMStudio、
> ClaudeCodeが止まっている間はOpenCodeが継続する」**

非エンジニアの単独開発であることを前提に、品質を落とさずトークン上限を回避する。

## 役割マップ

| 作業 | 担当 | 理由 |
|------|------|------|
| アーキテクチャ・設計方針 | ClaudeCode | 文脈判断が中心 |
| 機能開発・新規テンプレ作成 | ClaudeCode | 創造性・ドメイン理解が必要 |
| 収益化・グロース施策 | ClaudeCode | ビジネス判断が中心 |
| バグ調査・エラー解決 | ClaudeCode | 原因特定に推論が必要 |
| 変異テストの「設計」 | ClaudeCode | 「どの失敗経路を守るか」は判断 |
| 変異テストの「大量登録」 | LMStudio | 登録の定型化・パターン踏襲が可能 |
| 問題テンプレの機械的追加 | LMStudio | 既存パターンの踏襲が中心 |
| JSON・データ整備 | LMStudio | 決定的な整形作業 |
| コード要約・文書化 | LMStudio | 文脈再構成の補助（トークン節約） |
| 大量の一括リファクタ | LMStudio | 機械的・低リスクなものに限る |
| 上限中の継続作業 | OpenCode | 状態ファイルから再開 |

## LMStudioに任せてはいけないもの

1. **検査の「設計意図」** — 「この失敗経路に検査を足すべきか」はドメイン判断。定型登録とは分離する
2. **マージ・ブランチ精算の判断** — どのブランチを残すかはプロダクト判断
3. **稼働データの解釈** — 数値の意味付けはあなたとClaudeCode
4. **収益化・グロースの意思決定**

LMStudioは「**決定済みの方針の実行**」に徹する。方針はClaudeCode（またはOpenCode）が決める。

## 運用フロー

### 通常時
```
あなた（方針・指示）
   └→ ClaudeCode：判断・設計・実装
   └→ LMStudio：定型作業の大量実行（割り振りはあなた or ClaudeCode）
```

### ClaudeCode上限時
```
あなた（方針・指示）
   └→ OpenCode：ClaudeCodeの代役（PROJECT_STATE読解から再開）
   └→ LMStudio：定型作業継続
```

## 引き継ぎの仕組み

- **このファイル** に役割分担を固定
- **PROJECT_STATE.md**（未作成・要作成）に「現在の状況・次にやること・設計意図」を記録
  - ClaudeCodeが更新する / OpenCodeもLMStudioもこれを読んで同じ文脈から始める
- **CLAUDE.md / AGENTS.md** に共有運用ルール
- 区切りごとに会話要約を保存

## 注意

- 役割分担は**固定ではなく見直すもの**。プロジェクトの状況・ツールの性能に応じて更新
- LMStudioの品質が落ちたら担当物をClaudeCodeに戻す
